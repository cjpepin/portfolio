# LingoLeaf SPA deep links are handled by functions/lingoleaf/[[path]].ts.
# Do not add /lingoleaf/* rewrites here — they also match /lingoleaf/assets/* and break JS/CSS.

# lingoleaf-web is built with base `/lingoleaf/`; bare path must redirect.
/lingoleaf /lingoleaf/ 308

# Retire stale shell URL from an earlier deploy experiment.
/lingoleaf-spa /lingoleaf/ 301
/lingoleaf-spa.html /lingoleaf/ 301

# Trellis portfolio demo (desktop preview workspace in browser)
/trellis/demo/embed/*  /trellis/demo/embed/index.html  200

# Global//Debt — redirect legacy landing path to the app (opens without login)
/global-debt /global-debt/app/ 308
/global-debt/ /global-debt/app/ 308
/global-debt/app /global-debt/app/ 308
