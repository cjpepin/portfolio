# Cloudflare rejects rewrites to */index.html under the same splat prefix (infinite loop).
# Serve copied SPA shells from the public root instead; asset URLs stay under /lingoleaf/*.
/lingoleaf/demo/embed/*  /lingoleaf-demo-embed-spa.html  200
/lingoleaf/*  /lingoleaf-spa.html  200
