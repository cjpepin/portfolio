# Local wrangler.toml template. CI deploy overwrites this file with the
# project name from CF_PAGES_PROJECT_NAME before running wrangler pages deploy.
name = "portfolio"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
