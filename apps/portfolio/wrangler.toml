# Cloudflare Pages — run wrangler from this directory (not the monorepo root).
# Project name is passed via CF_PAGES_PROJECT_NAME in scripts/cloudflare-pages-deploy.mjs.
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
